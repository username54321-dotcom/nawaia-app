import { BookOpen, Plane, NotebookPen } from 'lucide-react-native';

export const GenreIcons = {
  كتب: <BookOpen size={18} color={'#404040'} />,
  رحلات: <Plane size={18} color={'#404040'} />,
  دورات: <NotebookPen size={18} color={'#404040'} />,
};
